{
  // Login configuration
  "Logging": {
    "LogLevel": {
      // Be default log level is Information
      "Default": "Information",

      // Log Level for ASP.NET Core framwork is Warning
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Database Connection String
  // Server = SHISH in SQL, NewUserRoles is the database we use
  "ConnectionStrings": {
    "Default": "Server=SHISH;Database=NewUserRoles;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=true"
  },

  // For now, allow all hosts
  "AllowedHosts": "*"
}
